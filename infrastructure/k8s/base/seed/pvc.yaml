apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seed-data
  namespace: finance
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName is patched per environment:
  #   local → standard (kind default)
  #   aws   → gp3
